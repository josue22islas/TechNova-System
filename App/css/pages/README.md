# Estilos exclusivos de cada vista

Aquí irán estilos que solo pertenecen a una página:

- `dashboard.css`
- `projects.css`
- `tasks.css`
- `clients.css`

Los estilos reutilizables continuarán separados por componente en las carpetas
CSS existentes.

